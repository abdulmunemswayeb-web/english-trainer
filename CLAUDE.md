# مدرّب الإنجليزية — التعليمات الدائمة

## عن صاحب المشروع

المستوى: pre-intermediate. الهدف **التحدث الفعلي**، لا القواعد النظرية ولا الامتحانات الأكاديمية.

مجالات الاستعمال الحقيقية:

- المعارض التجارية الدولية (جناح العرض، استقبال الزوار، المتابعة بعد المعرض)
- التفاوض مع الشركات والموردين (الأسعار، الكميات، الشحن، العقود)
- أساسيات السفر: المطار، الفنادق، المطاعم
- التعرّف على الناس وتكوين صداقات

## هيكل المشروع

```
index.html            التطبيق كاملاً (ملف واحد، بلا خادم، صالح لـ GitHub Pages)
sources/              ملفات PDF و Word يضعها المستخدم
data/questions.json   بنك الأسئلة
data/processed.json   سجل الملفات المعالَجة
CLAUDE.md             هذا الملف
```

بنك الأسئلة `data/questions.json` هو **المرجع الوحيد**. يقرأه التطبيق عبر `fetch` عند التشغيل
من خادم أو GitHub Pages. وللعمل عند فتح `index.html` مباشرة من القرص (`file://`) توجد نسخة
احتياطية داخل `index.html` بين العلامتين `SEED-BANK-START` و`SEED-BANK-END`.
**بعد كل تعديل على `data/questions.json` حدّث تلك النسخة لتطابقه.**

## كيف تكون القواعد

لا تشرح القاعدة كقاعدة. اعطِ جملة إنجليزية فيها فراغ داخل موقف حقيقي من عالم المستخدم
(جناح معرض، اجتماع مورّد، مكتب استقبال فندق)، وأربعة خيارات، وبعد الإجابة **سطر واحد فقط**
يوضح المعنى — مثل: `have been = بدأ في الماضي ومستمر`.

ركّز على ما يخرب المعنى في الكلام: الأزمنة الثلاثة الأساسية، الأسئلة، النفي، حروف الجر،
الطلب المهذب (could/would)، المقارنة، الشرط الأول.

اربط كل قاعدة بموقف، وكرّر نفس القاعدة في مواقف مختلفة.

## المفردات

كلمة جديدة واحدة على الأقل يومياً، مع مراجعة القديمة تلقائياً. لكل كلمة: النطق،
الترجمة العربية، وجملة مثال من مجال المستخدم.

## نظام المراجعة

خمسة صناديق Leitner. الخطأ يرجّع البطاقة للصندوق الأول وتُعاد في نفس الجلسة.
الصح ينقلها للأعلى بفواصل: يوم، ٣ أيام، أسبوع، ٣ أسابيع.
الجلسة عشرة أسئلة تُختار حسب الاستحقاق. التقدّم محفوظ في `localStorage`.

## أنواع الأسئلة

معنى كلمة (`word`) · إكمال فراغ (`fill`) · ترجمة من العربية يكتبها المستخدم (`translate`) ·
ترتيب كلمات لتكوين جملة (`order`).

## معالجة الملفات الجديدة

عند وجود ملف في `sources/` غير مسجّل في `data/processed.json`:

1. اقرأه واستخرج المفردات والتعابير والتراكيب المفيدة لأهدافي (معارض، أعمال، سفر، تعارف)
2. تجاهل ما هو أقل من مستواي أو خارج نطاقي
3. راجع `data/questions.json` ولا تكرّر الموجود
4. ولّد أسئلة بالأنواع الأربعة، ووسم كل سؤال بـ `"src"` باسم الملف
5. أضفها إلى `data/questions.json` دون حذف أي شيء قديم
6. سجّل الملف في `data/processed.json` مع التاريخ وعدد الأسئلة
7. أعطني ملخّصاً: كم سؤالاً أُضيف ومن أي مواضيع

## لا تقتصر على الملفات

الملفات مصدر إضافي، لكنك تعرف مصادر أفضل مني: قوائم الكلمات الأكثر استخداماً،
التعابير الشائعة في التفاوض التجاري، والأخطاء المتكررة عند الناطقين بالعربية.

في كل جلسة معالجة:

- وازن بين ما استخرجته من الملف وما تراه ناقصاً في بنك أسئلتي
- إذا كان الملف ضعيفاً أو مكرراً، قُل لي ذلك بصراحة وعوّض من عندك
- حدّد الفجوات: أي مواقف لم تُغطَّ؟ أي قواعد ناقصة؟
- أضف ما يسدّها حتى لو لم يرد في أي ملف

## الأخطاء الشائعة عند العرب

تستحق أسئلة خاصة ومتكررة: نسيان أداة النكرة (a/an)، الخلط بين in/on/at،
المضارع البسيط بدل المستمر، ترتيب الصفة والموصوف، الخلط بين make/do،
والخلط بين between/among.

## التكيّف

راجع تقدّمي المحفوظ. إن تكرر خطئي في نوع معيّن، ولّد أسئلة إضافية عليه.

## صيغة السؤال في `data/questions.json`

```jsonc
{
  "id": "f013",              // معرّف فريد: w=كلمة، f=فراغ، t=ترجمة، o=ترتيب
  "type": "fill",            // word | fill | translate | order
  "topic": "supplier",       // exhibition | supplier | hotel | restaurant | airport | networking
  "skill": "grammar:prepositions",  // للتكيّف: تجميع الأخطاء حسب المهارة
  "situation": "الموقف بالعربية قبل السؤال",   // لـ fill
  "text": "We ___ in this market since 2019.", // لـ fill
  "prompt": "الجملة العربية المطلوب ترجمتها",   // لـ translate و order
  "word": "booth", "ipa": "/buːθ/",            // لـ word
  "example": "...", "exampleAr": "...",        // لـ word
  "options": ["...", "...", "...", "..."],     // لـ word و fill
  "tokens": ["Could", "you", "send"],          // لـ order — مجموعها = answer
  "answer": "الإجابة الصحيحة",
  "accept": ["صيغ بديلة مقبولة بحروف صغيرة بلا ترقيم"],  // لـ translate
  "note": "سطر واحد يوضح المعنى",
  "src": "seed"              // seed أو اسم الملف في sources/
}
```

قواعد التحقق قبل أي إضافة: المعرّفات فريدة · `answer` موجود ضمن `options` في `word` و`fill` ·
`tokens` مجموعة بمسافات تساوي `answer` في `order` · لكل سؤال `note` من سطر واحد.
